import pydevcards
from pydevcards import mute, solo
from pprint import pprint, pformat

import math

import numpy as np
np.set_printoptions(precision=4, suppress=True)

################################################################################


def devcard_Sandbox(c: pydevcards.Devcard):
    c.m("Ready to go!")
